/**
 * SystemGrain — RETIRED VISUAL (visual migration to the HTML
 * reference system). The cinematic film-grain overlay conflicts
 * with the flat ivory editorial surface of the HTML reference.
 *
 * The component stays mounted in the canonical provider tree
 * (docs/SYSTEM_ARCHITECTURE.md §3) so the tree is not reordered;
 * it simply renders nothing.
 */
export function SystemGrain() {
  return null;
}
