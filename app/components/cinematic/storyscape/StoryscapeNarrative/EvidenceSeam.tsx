"use client";

/**
 * EvidenceSeam — ACT A exit silence (Scene 5, T01).
 *
 * Seam toward the Showcase act. The forces do not navigate: they
 * prepare (architecture §12 — Storyscape has ZERO exits; live force
 * readings are Brain territory whose door already existed in HC-03).
 *
 * The silence before the first subject-image of the page is the
 * abstinence that prepares the banquet (§10): the retina arrives at
 * the Showcase hungry.
 *
 * Per .rules/narrative-density-system.mdc T01 SILENCE: empty
 * breathing room, no content, no affordances.
 */
export function EvidenceSeam() {
  return (
    <section
      data-storyscape-scene="seam"
      data-density-tier="T01"
      aria-hidden
      className="relative w-full"
      style={{ minHeight: "30vh" }}
    />
  );
}
