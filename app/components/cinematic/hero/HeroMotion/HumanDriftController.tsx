"use client";

/**
 * HumanDriftController — formerly applied a sub-pixel breathing loop
 * (±0.5px x, sine.inOut, infinite yoyo) to the Hero title group.
 *
 * NEUTRALIZED during the atmospheric motion restraint pass: a perpetual
 * loop without narrative value on the establishing typography reads as
 * leakage against the "quiet, heavy, premium, stable" target. Stillness
 * carries more cinematic weight than micro-motion on the project's
 * establishing display block.
 *
 * Component kept (no architecture change, provider tree intact) but the
 * effect is a no-op. Re-activation, if ever needed, must be justified
 * against narrative-density-system.mdc + DESIGN_DNA marker 4 explicitly
 * (Atmospheric Realism does not equal perpetual movement on type).
 */
export function HumanDriftController() {
  return null;
}
