"use client";

/**
 * ReflectionBreath — ACT B Scene 2 (T02): the proof exhale.
 *
 * Contemplative pause between the protagonist threshold and the two
 * compact confirmations. The evidence is presented with the calm of
 * those who have more (architecture §8) — proof in a hurry looks
 * like a sale.
 *
 * Per .rules/narrative-density-system.mdc T02 CONTEMPLATIVE: minimal
 * surface, breathing room, no affordances.
 */
export function ReflectionBreath() {
  return (
    <section
      data-showcase-scene="breath"
      data-density-tier="T02"
      aria-hidden
      className="relative w-full"
      style={{ minHeight: "4vh" }}
    />
  );
}
